"""Shared utilities for the teaching Streamlit app.

Submodules:
    data_generator  -- cached random dataset generation (UI agent)
    db_utils        -- PostgreSQL/SQLite connection + CRUD (Database agent)
    compute_utils   -- serial/parallel/vectorized benchmarks (Parallelization agent)
"""
